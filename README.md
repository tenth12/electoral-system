# ระบบเลือกตั้งอิเล็กทรอนิกส์ (Electronic Electoral System)

โครงการพัฒนาระบบเลือกตั้งอิเล็กทรอนิกส์สำหรับคณะวิทยาการคอมพิวเตอร์ ทำงานผ่านเครือข่ายอินเทอร์เน็ตที่ช่วยประหยัดเวลาและทรัพยากรในการจัดตั้งหน่วยเลือกตั้ง รองรับการใช้งานของนักศึกษาและผู้สมัครรับเลือกตั้งได้อย่างเต็มรูปแบบ

## 🌟 จุดเด่นของระบบ
- **ระบบยืนยันตัวตน (Authentication):** ป้องกันการสวมสิทธิ์และยืนยันการลงคะแนนได้ 1 สิทธิ์ต่อ 1 ผู้ใช้งานเท่านั้น
- **หน้าจอสำหรับผู้สมัคร (Candidate Dashboard):** ผู้สมัครลงรับเลือกตั้งสามารถลงทะเบียน อัปโหลดรูปภาพ และแก้ไขประวัติ นโยบายของตัวเองได้
- **ระบบจัดการสำหรับแอดมิน (Admin Panel):** ผู้ดูแลระบบสามารถตรวจสอบรายชื่อผู้ใช้งาน จัดการประกาศ ควบคุมการเปิด-ปิดระบบโหวต และดูผลคะแนนแบบเรียลไทม์ได้
- **หน้าโหวตที่ใช้งานง่าย (Voting Interface):** แสดงรายชื่อผู้สมัคร นโยบาย รูปภาพ และกดยืนยันการโหวตได้อย่างรวดเร็ว

## 💻 เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend (ระบบหน้าบ้าน)
- **Framework:** Next.js (App Router)
- **Styling:** Tailwind CSS
- **Features:** 
  - การจัดการ State ด้วย React Hooks
  - รองรับการแสดงผลแบบ Responsive Design
  - เชื่อมต่อ API ด้วย Fetch & JWT Authentication
  - การป้องกันสแปมด้วย Cloudflare Turnstile

### Backend (ระบบหลังบ้าน)
- **Framework:** NestJS (Node.js)
- **Database:** MongoDB (using Mongoose ORM)
- **Authentication:** JWT (JSON Web Tokens), Passport.js
- **Features:**
  - สร้างและตรวจสอบ JWT token
  - API แนวทาง RESTful แยกส่วนข้อมูล (Users, Candidates, Votes, Settings)
  - ระบบตรวจสอบและอัปโหลดไฟล์ (File Upload)
  - เข้ารหัสรหัสผ่านเพื่อความปลอดภัยของฐานข้อมูล

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Installation Guide)

### สิ่งที่ต้องมีเบื้องต้น (Prerequisites)
- [Node.js](https://nodejs.org/) (เวอร์ชั่น 18 หรือใหม่กว่า)
- [MongoDB](https://www.mongodb.com/) (หรือ MongoDB Atlas)
- Git

### 1. การตั้งค่า Backend
1. เข้าไปในโฟลเดอร์ Backend
   ```bash
   cd login_auth_backend
   ```
2. ติดตั้ง Dependencies
   ```bash
   npm install
   ```
3. สร้างไฟล์ `.env` ในโฟลเดอร์ `login_auth_backend` และกำหนดค่าตัวแปร:
   ```env
   # ตัวอย่างไฟล์ .env
   MONGODB_URI=mongodb://localhost:27017/electoral_system
   JWT_SECRET=your_super_secret_jwt_key
   PORT=3000
   ```
4. รันเซิร์ฟเวอร์
   ```bash
   npm run start:dev
   ```

### 2. การตั้งค่า Frontend
1. เปิด Terminal ใหม่แล้วเข้าไปในโฟลเดอร์ Frontend
   ```bash
   cd frontend
   ```
2. ติดตั้ง Dependencies
   ```bash
   npm install
   ```
3. สร้างไฟล์ `.env.local` ในโฟลเดอร์ `frontend` และกำหนดค่า API URL:
   ```env
   NEXT_PUBLIC_API_URL=http://localhost:3000
   ```
4. รันแอปพลิเคชันฝั่ง Frontend
   ```bash
   npm run dev
   ```

หน้าเว็บจะพร้อมใช้งานที่ `http://localhost:3001` (หรือ port ที่ Next.js แจ้งทำงาน)

## 🎯 โครงสร้างโฟลเดอร์ที่สำคัญ (Project Structure)
- `/frontend/app` - เก็บโค้ดสำหรับหน้า Web UI ทั้งระบบ (vote, admin, candidates, auth)
- `/frontend/app/services` - ไฟล์สำหรับจัดการการดึงข้อมูลจาก API (เช่น auth.service.ts)
- `/frontend/app/components` - ชิ้นส่วน UI ที่นำมาใช้ซ้ำ (เช่น Navbar)
- `/login_auth_backend/src` - โค้ด Backend ของ NestJS แยกตาม Module (auth, users, candidates, votes)

## 👥 ผู้จัดทำโครงการ
โปรเจกต์นี้เป็นส่วนหนึ่งของรายวิชาระบบเลือกตั้ง (กลุ่ม 5 คน) คณะวิทยาการคอมพิวเตอร์
